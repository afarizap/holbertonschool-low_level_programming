#include <stdio.h>
#include "holberton.h"
/**
 * _isupper - works as filter for uppercase
 * @c: - character to execute
 * Return: 1 - gives uppercase
 *
 */
int _isupper(int c)

{
	if (c < 91)
	{
		if (c > 64)
		{
			return (1);
		}
	}
	else
	{
		return (0);
	}
	return (0);
}
